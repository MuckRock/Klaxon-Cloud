# Squarelet OIDC public client credentials for the Klaxon extension.
#
# Register a public client (client_type=public, PKCE) in Squarelet admin and
# put the resulting client_id here. No client_secret — public clients don't use
# one. The extension's redirect URI must be registered with the client too;
# it's visible in the service worker console via chrome.identity.getRedirectURL().

MUCKROCK_ACCOUNTS_HOST="https://dev.squarelet.com/"
MUCKROCK_CLIENT_ID=""
MUCKROCK_SCOPES="openid profile email uuid organizations"

# DocumentCloud API base URL (with trailing slash)
MUCKROCK_DOCUMENTCLOUD_API="https://api.www.documentcloud.org/api/"
# Klaxon add-on ID in DocumentCloud
MUCKROCK_KLAXON_ID=""

# Companion web app origin. The web-bridge content script is declared to run
# only on this origin (extension detection + "create alert" handoff). Set per
# environment — dev http://localhost:5173, prod https://klaxon.muckrock.com.
# Leave empty to omit the content script from the build entirely.
MUCKROCK_WEB_ORIGIN="http://localhost:5173"

# Deploy environment — one of production | staging | development. Baked in at
# build time (Vite `envPrefix: "MUCKROCK_"`). Used by Sentry as its `environment`
# tag, and available for any other integration that wants to know which build
# this is.
MUCKROCK_ENVIRONMENT="development"

# Sentry error reporting (issue #110). DSN comes from the "Klaxon Extension"
# Sentry project — safe to embed in the bundle (Sentry DSNs are designed to be
# public). Leave blank to disable Sentry entirely for local dev.
# Note: Sentry only initializes in the sidepanel and background service worker;
# the on-page content script (page.js) is deliberately excluded.
MUCKROCK_SENTRY_DSN=""
